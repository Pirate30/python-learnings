# SET - unordered - unindexed
# can be deleted - can not be changed
# {}
# unique elements - values can not be repeated - will approve only first occurence of the element

s = {1, 2, 3, 4.5, 5, 3}
print(type(s))

# Set - iteration

for x in s:
    print(x)
